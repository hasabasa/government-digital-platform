# 🎨 Фронтенд платформы госслужащих

React PWA приложение в стиле Telegram с современным дизайном и адаптивным интерфейсом для государственных служащих Казахстана.

## 🚀 Быстрый старт

```bash
cd frontend-shell
npm install
npm run dev
```

Откройте http://localhost:3000 в браузере.

## 🛠️ Технологический стек

### Основные технологии
- **React 18** - UI библиотека с hooks
- **TypeScript** - типизация и безопасность
- **Vite** - быстрый сборщик и dev сервер
- **TailwindCSS** - utility-first CSS фреймворк
- **React Router** - клиентская маршрутизация

### State Management
- **Zustand** - легкое управление состоянием
- **React Query** - кэширование серверного состояния
- **Axios** - HTTP клиент с интерцепторами

### Real-time
- **Socket.IO Client** - WebSocket соединения
- **React Context** - WebSocket провайдер

### UI/UX
- **Lucide React** - современные иконки
- **Framer Motion** - плавные анимации
- **React Hot Toast** - уведомления
- **React Hook Form** - обработка форм

### PWA
- **Vite PWA Plugin** - Service Worker
- **Workbox** - кэширование стратегии
- **Web App Manifest** - установка приложения

## 🎨 Дизайн в стиле Telegram

### Цветовая схема
```css
/* Основные цвета */
--primary: #2D7DD2        /* Синий с бирюзой */
--primary-dark: #1E5FA5   /* Темный синий */
--secondary: #F4A261      /* Оранжевый акцент */

/* Темы */
--bg-light: #F8FAFC       /* Светлый фон */
--bg-dark: #1E293B        /* Темный фон */
--text-light: #1F2937     /* Темный текст */
--text-dark: #F9FAFB      /* Светлый текст */
```

### UI особенности
- **Двухпанельный layout** на десктопе
- **Однопанельная навигация** на мобильных
- **Telegram-подобные сообщения** с группировкой
- **Адаптивные аватары** с инициалами
- **Smooth анимации** переходов

## 📁 Структура проекта

```
frontend-shell/
├── src/
│   ├── components/           # React компоненты
│   │   ├── ui/              # Базовые UI элементы
│   │   │   ├── Avatar.tsx   # Аватары
│   │   │   ├── Button.tsx   # Кнопки
│   │   │   ├── Input.tsx    # Поля ввода
│   │   │   └── ...
│   │   ├── chat/            # Компоненты чата
│   │   │   ├── ChatHeader.tsx
│   │   │   ├── MessageList.tsx
│   │   │   ├── MessageItem.tsx
│   │   │   └── MessageComposer.tsx
│   │   ├── sidebar/         # Боковая панель
│   │   │   ├── Sidebar.tsx
│   │   │   ├── ChatList.tsx
│   │   │   └── ChatFilters.tsx
│   │   └── layout/          # Layout компоненты
│   │       └── MainLayout.tsx
│   ├── pages/               # Страницы
│   │   ├── LoginPage.tsx
│   │   └── MainPage.tsx
│   ├── stores/              # Zustand хранилища
│   │   ├── auth.store.ts    # Аутентификация
│   │   ├── chat.store.ts    # Чаты и сообщения
│   │   └── theme.store.ts   # Темизация
│   ├── services/            # API сервисы
│   │   ├── api.service.ts   # HTTP API
│   │   └── websocket.service.ts
│   ├── providers/           # React провайдеры
│   ├── hooks/               # Кастомные хуки
│   ├── utils/               # Утилиты
│   ├── styles/              # Стили
│   └── types/               # Типы
├── public/                  # Статические файлы
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

## 🔧 Команды разработки

```bash
# Разработка
npm run dev              # Запуск dev сервера
npm run build            # Сборка для продакшена
npm run preview          # Просмотр prod сборки

# Качество кода
npm run lint             # ESLint проверка
npm run lint:fix         # Автофикс ESLint
npm run type-check       # TypeScript проверка

# Тестирование
npm run test             # Unit тесты
npm run test:watch       # Тесты в watch режиме
npm run test:coverage    # Покрытие тестами

# Анализ
npm run analyze          # Анализ размера бандла
```

## 🧩 Основные компоненты

### UI Компоненты

#### Avatar
```tsx
<Avatar 
  src="/user-avatar.jpg"
  name="Иван Петров"
  size="md"
  status="online"
  onClick={() => console.log('Avatar clicked')}
/>
```

#### Button  
```tsx
<Button 
  variant="primary"
  size="md"
  loading={isLoading}
  icon={<Send />}
  onClick={handleClick}
>
  Отправить
</Button>
```

#### Input
```tsx
<Input
  label="Email"
  type="email"
  placeholder="Введите email"
  error={errors.email}
  icon={<Mail />}
/>
```

### Chat Компоненты

#### MessageList
- Виртуализация для больших чатов
- Группировка сообщений по дате и отправителю
- Автоскролл к новым сообщениям
- Lazy loading старых сообщений

#### MessageComposer
- Автоизменение высоты textarea
- Прикрепление файлов с preview
- Отправка по Enter (Shift+Enter для новой строки)
- Typing indicators

### Layout

#### MainLayout
- Адаптивный двух/однопанельный дизайн
- Sidebar overlay на мобильных
- Swipe gestures для навигации
- Keyboard shortcuts

## 🔄 State Management

### Auth Store
```typescript
const { user, token, isAuthenticated, login, logout } = useAuthStore();
```

### Chat Store  
```typescript
const { 
  chats, 
  activeChat, 
  messages, 
  searchQuery,
  setActiveChat,
  addMessage 
} = useChatStore();
```

### Theme Store
```typescript
const { theme, setTheme, toggleTheme } = useThemeStore();
```

## 🌐 API интеграция

### HTTP запросы
```typescript
// Автоматическое добавление токенов
const response = await apiService.getUserProfile();

// Автообновление токенов при 401
const chats = await apiService.getUserChats();
```

### WebSocket
```typescript
// Real-time сообщения
socket.on('message:new', (message) => {
  addMessage(message.chatId, message);
});

// Typing indicators
socket.emit('typing_start', { chatId });
```

## 📱 PWA функции

### Service Worker
- Кэширование статических ресурсов
- Network-first для API запросов
- Offline page для недоступности сети
- Background sync для отложенных запросов

### Manifest
- Установка как нативное приложение  
- Кастомные иконки и splash screen
- Standalone режим отображения
- Share target для получения файлов

### Offline поддержка
- Кэширование критических данных
- Очередь сообщений для отправки при подключении
- Индикатор состояния сети
- Graceful degradation функций

## 🎯 Особенности UX

### Адаптивность
- **Desktop (lg+)**: Двухпанельный вид
- **Tablet (md-lg)**: Адаптивные панели
- **Mobile (<md)**: Однопанельная навигация

### Telegram-подобное поведение
- Группировка сообщений от одного отправителя
- Время сообщений в формате HH:mm
- Статусы доставки для исходящих сообщений
- Typing indicators в реальном времени

### Темизация
- Автоматическое определение системной темы
- Плавные переходы между темами
- Сохранение выбора в localStorage
- WCAG совместимые цветовые контрасты

## 🧪 Тестирование

### Unit тесты (Vitest)
```bash
npm run test
```

### Component тестирование
```typescript
import { render, screen } from '@testing-library/react';
import { Avatar } from './Avatar';

test('displays user initials when no image', () => {
  render(<Avatar name="Иван Петров" />);
  expect(screen.getByText('ИП')).toBeInTheDocument();
});
```

### E2E тесты (планируется)
- Playwright для комплексного тестирования
- Тестирование user flows
- Visual regression тесты

## ♿ Accessibility

### WCAG 2.1 AA соответствие
- Цветовые контрасты 4.5:1+
- Keyboard navigation поддержка
- Screen reader compatibility
- Focus management
- ARIA labels и roles

### Возможности
- High contrast mode
- Reduced motion preferences
- Zoom до 200% без потери функциональности
- Voice control готовность

## 📈 Производительность

### Оптимизации
- Code splitting по маршрутам
- Lazy loading компонентов
- React.memo для предотвращения ререндеров
- Виртуализация списков
- Image lazy loading
- Bundle size оптимизация

### Метрики
- FCP (First Contentful Paint): <1.5s
- LCP (Largest Contentful Paint): <2.5s  
- FID (First Input Delay): <100ms
- CLS (Cumulative Layout Shift): <0.1

## 🚢 Развертывание

### Development
```bash
npm run dev
```

### Production
```bash
npm run build
npm run preview
```

### Docker
```bash
docker build -t frontend-shell .
docker run -p 3000:80 frontend-shell
```

## 🔗 Интеграция с бэкендом

### API подключение
```typescript
// vite.config.ts
export default {
  server: {
    proxy: {
      '/api': 'http://localhost:8080',
      '/socket.io': {
        target: 'http://localhost:8080',
        ws: true
      }
    }
  }
}
```

### Environment variables
```bash
# .env
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_WS_URL=http://localhost:8080
```

## 📚 Документация

- **[Полное описание фронтенда](../FRONTEND_DESCRIPTION.txt)** - детальная техническая документация
- **[Storybook](http://localhost:6006)** - документация компонентов (планируется)
- **[Design System](./docs/design-system.md)** - гайдлайны дизайна

---

**🎨 Красивый, быстрый и доступный!** Запустите `npm run dev` и начинайте разработку интерфейса!
