# Government Digital Platform

Платформа для государственных служащих с интерфейсом в стиле Telegram, интегрированная с backend API.

## 🚀 Возможности

- **Аутентификация**: ЭЦП, eGov Mobile, обычный логин
- **Чаты и сообщения**: Приватные чаты, групповые чаты, каналы
- **Управление задачами**: Kanban доска в стиле Trello
- **Приказы и распоряжения**: Создание и управление приказами
- **Отчеты**: Система отчетности с ролевым доступом
- **Файловый менеджер**: Загрузка, скачивание, предпросмотр файлов
- **Real-time обновления**: WebSocket интеграция
- **Ролевой доступ**: Министры, руководители отделов, сотрудники

## 🏗️ Архитектура

### Frontend
- **React 18** с TypeScript
- **Vite** для сборки
- **Tailwind CSS** для стилизации
- **Zustand** для управления состоянием
- **React Query** для кэширования API
- **React Router** для навигации

### Backend Integration
- **REST API** для всех операций
- **WebSocket** для real-time обновлений
- **JWT** аутентификация с refresh токенами
- **Ролевой доступ** на уровне API

## 📁 Структура проекта

```
src/
├── components/          # UI компоненты
├── stores/             # Zustand stores
├── services/           # API и WebSocket сервисы
├── types/              # TypeScript типы
├── styles/             # CSS стили
└── main.tsx           # Точка входа
```

## 🔧 Установка и запуск

### Требования
- Node.js 16+
- npm или yarn

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

### Сборка для продакшена
```bash
npm run build
```

### Проверка типов
```bash
npm run type-check
```

## 🔌 Интеграция с Backend

### Переменные окружения
Создайте файл `.env.local`:

```env
REACT_APP_API_URL=http://localhost:8080/api/v1
REACT_APP_WS_URL=ws://localhost:8080/ws
```

### API Endpoints

#### Аутентификация
- `POST /api/v1/auth/login` - Обычный логин
- `POST /api/v1/auth/login-ecp` - Вход по ЭЦП
- `POST /api/v1/auth/login-egov-mobile` - Вход через eGov Mobile
- `POST /api/v1/auth/refresh` - Обновление токена
- `POST /api/v1/auth/logout` - Выход
- `GET /api/v1/auth/me` - Текущий пользователь

#### Пользователи
- `GET /api/v1/users/profile` - Профиль пользователя
- `PUT /api/v1/users/profile` - Обновление профиля
- `GET /api/v1/users/search` - Поиск пользователей
- `GET /api/v1/users/{id}` - Получение пользователя по ID

#### Чаты
- `GET /api/v1/chats/user` - Чаты пользователя
- `POST /api/v1/chats` - Создание чата
- `GET /api/v1/chats/{id}` - Получение чата
- `PUT /api/v1/chats/{id}` - Обновление чата
- `DELETE /api/v1/chats/{id}` - Удаление чата
- `POST /api/v1/chats/messages` - Отправка сообщения
- `GET /api/v1/chats/{id}/messages` - Сообщения чата

#### Задачи
- `GET /api/v1/tasks` - Список задач
- `POST /api/v1/tasks` - Создание задачи
- `GET /api/v1/tasks/{id}` - Получение задачи
- `PUT /api/v1/tasks/{id}` - Обновление задачи
- `DELETE /api/v1/tasks/{id}` - Удаление задачи
- `PUT /api/v1/tasks/{id}/status` - Изменение статуса

#### Приказы
- `GET /api/v1/orders` - Список приказов
- `POST /api/v1/orders` - Создание приказа
- `GET /api/v1/orders/{id}` - Получение приказа
- `PUT /api/v1/orders/{id}` - Обновление приказа
- `DELETE /api/v1/orders/{id}` - Удаление приказа

#### Отчеты
- `GET /api/v1/reports` - Список отчетов
- `POST /api/v1/reports` - Создание отчета
- `GET /api/v1/reports/{id}` - Получение отчета
- `PUT /api/v1/reports/{id}` - Обновление отчета
- `DELETE /api/v1/reports/{id}` - Удаление отчета

#### Файлы
- `POST /api/v1/files/upload` - Загрузка файла
- `POST /api/v1/files/upload-multiple` - Загрузка нескольких файлов
- `GET /api/v1/files/user` - Файлы пользователя
- `GET /api/v1/files/{id}` - Получение файла
- `GET /api/v1/files/{id}/download` - Скачивание файла
- `GET /api/v1/files/{id}/preview` - Предпросмотр файла

### WebSocket Events

#### Чаты
- `message:new` - Новое сообщение
- `message:updated` - Обновление сообщения
- `message:deleted` - Удаление сообщения
- `typing:start` - Начало печати
- `typing:stop` - Окончание печати

#### Задачи
- `task:created` - Создание задачи
- `task:updated` - Обновление задачи
- `task:deleted` - Удаление задачи
- `task:status_changed` - Изменение статуса

#### Отчеты
- `report:created` - Создание отчета
- `report:updated` - Обновление отчета
- `report:deleted` - Удаление отчета
- `report:status_changed` - Изменение статуса

## 👥 Ролевой доступ

### Министр
- Полный доступ ко всем функциям
- Может отправлять сообщения всем руководителям отделов
- Может создавать приказы для всех
- Может создавать задачи для любого сотрудника

### Руководитель отдела
- Доступ к функциям своего отдела
- Может отправлять сообщения сотрудникам отдела
- Может создавать приказы для отдела
- Может создавать задачи для сотрудников отдела

### Руководитель секции
- Доступ к функциям своей секции
- Может отправлять сообщения сотрудникам секции
- Может создавать задачи для секции

### Сотрудник
- Доступ к новостной ленте
- Может отправлять сообщения коллегам по секции
- Может создавать личные задачи
- Ограниченный доступ к приказам

## 🎨 UI/UX Особенности

- **Telegram-стиль интерфейс** с боковой панелью
- **Адаптивный дизайн** для мобильных устройств
- **Темная/светлая тема** с автоматическим переключением
- **Анимации и переходы** для плавного взаимодействия
- **Интуитивная навигация** с нижней панелью на мобильных

## 🧪 Тестирование

### Запуск тестов
```bash
npm test
```

### Проверка покрытия
```bash
npm run test:coverage
```

## 📦 Развертывание

### Netlify
1. Подключите репозиторий к Netlify
2. Установите build command: `npm run build`
3. Установите publish directory: `dist`
4. Настройте переменные окружения

### Vercel
1. Подключите репозиторий к Vercel
2. Автоматическое развертывание при push в main
3. Настройте переменные окружения в dashboard

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 📞 Поддержка

По вопросам интеграции и поддержки обращайтесь к команде разработки.
