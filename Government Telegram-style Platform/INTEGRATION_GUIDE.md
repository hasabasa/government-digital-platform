# Руководство по интеграции Frontend с Backend

## 🎯 Обзор

Этот документ описывает процесс интеграции существующего frontend кода с backend API согласно требованиям из `front.txt` и `role.txt`.

## 📋 Что уже реализовано

### ✅ Frontend компоненты
- Все основные экраны (новости, задачи, чаты, приказы, отчеты)
- UI компоненты в стиле Telegram
- Адаптивный дизайн для мобильных устройств
- Ролевой выбор при входе

### ✅ Backend интеграция
- API сервис (`services/api.service.ts`)
- WebSocket сервис (`services/websocket.service.ts`)
- Zustand stores для управления состоянием
- TypeScript типы для всех сущностей
- Ролевой доступ и разрешения

## 🔧 Что нужно сделать

### 1. Установка зависимостей

```bash
cd "Government Telegram-style Platform"
npm install
```

### 2. Настройка переменных окружения

Файл `.env.local` уже создан с настройками для локальной разработки:

```env
REACT_APP_API_URL=http://localhost:8080/api/v1
REACT_APP_WS_URL=ws://localhost:8080/ws
```

### 3. Интеграция существующих компонентов

#### Обновление RoleSelection.tsx

Нужно заменить mock логику на реальную аутентификацию:

```typescript
// В RoleSelection.tsx заменить handleRoleSelect на:
const handleRoleSelect = async (role: string) => {
  try {
    // Здесь должна быть реальная аутентификация
    // Пока используем mock для демонстрации
    const mockUser = {
      id: '1',
      username: 'user',
      email: 'user@example.com',
      firstName: 'Иван',
      lastName: 'Иванов',
      role: role as UserRole,
      organization: 'Министерство',
      position: 'Сотрудник',
      isOnline: true,
      permissions: []
    };
    
    useAuthStore.getState().setUser(mockUser);
    setIsLoggedIn(true);
  } catch (error) {
    console.error('Login failed:', error);
  }
};
```

#### Обновление App.tsx

Нужно интегрировать с реальной аутентификацией:

```typescript
// В App.tsx заменить проверку isLoggedIn на:
const { isAuthenticated, user } = useAuthStore();

// И использовать user.role вместо userRole
const userRole = user?.role || 'employee';
```

### 4. Интеграция WebSocket

В каждом компоненте, который использует real-time обновления, добавить:

```typescript
useEffect(() => {
  // Подключение к WebSocket
  websocketService.connect();
  
  // Настройка обработчиков сообщений
  const unsubscribe = websocketService.onMessage((event) => {
    // Обработка событий
  });
  
  return () => {
    unsubscribe();
    websocketService.disconnect();
  };
}, []);
```

### 5. Интеграция API вызовов

#### В TasksScreen.tsx

```typescript
const { tasks, fetchTasks, createTask } = useTaskStore();

useEffect(() => {
  fetchTasks();
}, [fetchTasks]);

const handleCreateTask = async (taskData: Partial<Task>) => {
  try {
    await createTask(taskData);
    // Обновить список задач
  } catch (error) {
    console.error('Failed to create task:', error);
  }
};
```

#### В ChatListScreen.tsx

```typescript
const { chats, fetchChats } = useChatStore();

useEffect(() => {
  fetchChats();
}, [fetchChats]);
```

#### В OrdersScreen.tsx

```typescript
const { orders, fetchOrders, canCreateOrder } = useOrderStore();

useEffect(() => {
  fetchOrders();
}, [fetchOrders]);

// Показывать кнопку создания только если есть права
{canCreateOrder() && (
  <button onClick={handleCreateOrder}>Создать приказ</button>
)}
```

## 🚀 Запуск и тестирование

### 1. Запуск frontend

```bash
npm run dev
```

Frontend будет доступен по адресу: http://localhost:3000

### 2. Тестирование интеграции

#### Проверка аутентификации
1. Откройте http://localhost:3000
2. Выберите роль (например, "minister")
3. Проверьте, что вы вошли в систему

#### Проверка API вызовов
1. Откройте DevTools → Network
2. Выполните действия в приложении
3. Проверьте, что API запросы отправляются на правильные endpoints

#### Проверка WebSocket
1. Откройте DevTools → Console
2. Должны появиться сообщения о подключении к WebSocket
3. Проверьте статус соединения

### 3. Отладка

#### Проблемы с API
- Проверьте URL в `.env.local`
- Убедитесь, что backend запущен на порту 8080
- Проверьте CORS настройки backend

#### Проблемы с WebSocket
- Проверьте WebSocket URL в `.env.local`
- Убедитесь, что backend поддерживает WebSocket
- Проверьте аутентификацию WebSocket

## 📱 Тестирование на мобильных устройствах

### 1. Responsive дизайн
- Используйте DevTools → Toggle device toolbar
- Проверьте все экраны на разных размерах
- Убедитесь, что нижняя навигация работает

### 2. Touch события
- Проверьте все интерактивные элементы
- Убедитесь, что размеры кнопок достаточны для пальца
- Проверьте скроллинг на мобильных

## 🔒 Безопасность

### 1. Ролевой доступ
- Все проверки прав реализованы в stores
- API вызовы защищены на уровне frontend
- Backend должен дублировать все проверки

### 2. Аутентификация
- JWT токены хранятся в localStorage
- Автоматическое обновление токенов
- Логаут очищает все данные

### 3. Валидация данных
- Все пользовательские данные валидируются
- TypeScript обеспечивает типизацию
- API возвращает понятные ошибки

## 📊 Мониторинг и логирование

### 1. Логирование ошибок
```typescript
// В API сервисе
console.error('API request failed:', error);

// В WebSocket сервисе
console.error('WebSocket error:', error);
```

### 2. Мониторинг производительности
- Используйте React DevTools Profiler
- Мониторьте размер bundle
- Проверяйте время загрузки

## 🚀 Развертывание

### 1. Production build
```bash
npm run build
```

### 2. Переменные окружения для production
```env
REACT_APP_API_URL=https://your-api-domain.com/api/v1
REACT_APP_WS_URL=wss://your-api-domain.com/ws
```

### 3. Проверка production сборки
```bash
npm run preview
```

## 🔄 Обновления и поддержка

### 1. Обновление зависимостей
```bash
npm update
```

### 2. Проверка совместимости
```bash
npm run type-check
npm run lint
```

### 3. Тестирование после обновлений
- Запустите все тесты
- Проверьте работу в браузере
- Протестируйте на мобильных устройствах

## 📞 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера на ошибки
2. Проверьте Network tab на неудачные запросы
3. Убедитесь, что backend работает корректно
4. Проверьте переменные окружения
5. Обратитесь к команде разработки

## 🎉 Готово!

После выполнения всех шагов у вас будет полностью интегрированный frontend, который:

- ✅ Подключается к backend API
- ✅ Использует real-time обновления через WebSocket
- ✅ Реализует ролевой доступ
- ✅ Поддерживает все требуемые функции
- ✅ Имеет адаптивный дизайн
- ✅ Готов к production развертыванию
